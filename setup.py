from setuptools import setup

setup(name='statsdistribbrian',
      version = '0.1',
      description = "A package to load data and compute distribultional statistics, and visualization of histograms",
      packages = ['statsdistribbrian'],
      author = ['Brian Pinto'],
      license = 'The MIT License',
      zip_safe = False)
